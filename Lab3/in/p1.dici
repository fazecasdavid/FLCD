game 
{

    integer a;
    integer b;
    integer c;

    read(a);
    read(b);
    read(c);

    integer max;
    max = "abc";

    if (max <= b) {
        max = b;
    }

    if (max < c) {
        max = c;
    }

    write(max);

}
stop